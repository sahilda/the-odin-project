<h1>All Articles</h1>

<ul>
    <% @articles.each do |article| %>
        <li><%= link_to article.title, article_path(article.id), class: "article_title" %></li>
    <% end %>
</ul>

<%= link_to "Create a new Article", new_article_path, class: "new_article" %>
